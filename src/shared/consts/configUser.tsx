import { ThemeMode } from '@shared/types/theme'
import type { DefaultCustomProps } from '@shared/types/theme'

export const CONFIG_DEFAULT_USER: DefaultCustomProps = {
	fontFamily: 'DM Sans',
	mode: ThemeMode.LIGHT,
	presetColor: 'default',
	i18n: 'es',
}
